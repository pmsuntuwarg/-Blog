@model Blog.Models.Category

<div class="modal fade" id="delete-category-modal">
    <div class="modal-dialog" style="width:400px;" role="document">
        <div class="modal-content timeout-modal">
            <div class="modal-body">
                <button class="close" data-dismiss="modal" aria-label="Close"></button>
                <div class="text-center h3 mb-3">@Model.CategoryName</div>
                <form asp-controller="Category" asp-action="Delete" method="post">
                    <div id="timeout-activate-box">
                        <div class="text-center">
                            <input asp-for="CategoryId" hidden />
                            <input asp-for="CategoryName" hidden />
                            <button type="submit" class="btn btn-primary btn-fix btn-air">Delete</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>